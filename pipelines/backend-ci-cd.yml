trigger:
  branches:
    include:
      - your-backend-branch

pool:
  vmImage: ubuntu-latest

steps:
- script: echo "Backend pipeline is reading the correct YAML file"
  displayName: Test backend pipeline
